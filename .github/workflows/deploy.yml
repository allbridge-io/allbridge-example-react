name: Build and deploy to GitHub Pages

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎️
        uses: actions/checkout@v2.3.1

      - name: Install, build 🔧
        run: |
          npm install
          npm run build

      - name: Set Git config
        run: |
          git config user.email jenya476@gmail.com
          git config user.name Jenya476

      - name: Deploy 🚀
        run: npm run deploy
